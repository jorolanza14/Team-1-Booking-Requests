-- Initialize database schema for Diocese project

-- Create extension if not exists
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The tables will be created by Sequelize migrations
-- This file can be used for any initial setup that needs to happen
-- before the application runs its migrations